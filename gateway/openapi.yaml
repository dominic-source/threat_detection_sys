openapi: 3.1.0
info:
  title: Gateway API
  version: '0.1.0'
paths:
  /test:
    post:
      summary: Test endpoint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - user_id
              properties:
                user_id:
                  type: string
                foo:
                  type: string
      responses:
        '200':
          description: OK
